#import "lib.cp"
//#import "main.cp"
//#import "MappedFileLinux.cp"

main := !(int argc, char^^ argv) -> int
{
	//c := MappedFile("Test.cp")
	//for c printf("%c",it)

	if 0 == 0L printf("wu\n")

	return 0
}

