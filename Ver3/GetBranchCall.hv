TryGetBranchCall := !(Token^ itm) -> BoxExeObj^
{
	if itm.Down == null
		return null

	if itm.Down.GetValue() == "if"
		return TryParseIf(itm)
	if itm.Down.GetValue() == "for"
		return GetForCycle(itm)
	if itm.Down.GetValue() == "while"
		return TryParseWhile(itm)
	if itm.Down.GetValue() == "switch"
		return TryParseSwitch(itm)
	return null
}
