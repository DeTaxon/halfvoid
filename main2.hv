
internalTupleConstructor := !(@Tpl this,args...) -> void
{
	for it,i : args
	{
		this.i = it
	}
}

main := !(char^[] args) -> int
{
	test := LexBuilder
	test.ApplyReg("[a-zA-Z\\-\\.]+")
	m := test.GenerateMachine()
	m.PrintIt()

	gRepo := new vRepo
	gRepo.Init(".")

	tst := gRepo.GetFile("main2.hv")
	mp := tst.GetMap()

	// m.RegExpReadText(mp.Get(),mp.Size(), (a,b,c) ==> {
	// 	spn := StringSpan(mp.Get()[b]&,c)
	// 	printf("hm <%s>\n",spn.GetString().Str())
	// })
	
	return 0
}
