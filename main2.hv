
val := int
hm := !() -> !(int^&)& -> int&
{
	x := (int^& y) ==> int&
	{
		y = val&
		return val
	}
	return x.Capture()
}
main := !(char^[] args) -> int
{
	
	gRepo := new vRepo
	gRepo.Init(".")

	gRepo.ExamineFolder(gRepo.rootFolder)
	fil := gRepo.GetFile("main2.hv")
	mp := fil.GetMap()
	spn := StringSpan(mp.Get(),mp.Size())
	printf("res %s\n",spn.GetString().Str())


	return 0
}
