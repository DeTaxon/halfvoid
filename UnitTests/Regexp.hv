


TestRegexp := !() -> void
{
	ind := "[a-z]+[a-z0-9]*"reg

	assert("abc01" in ind)
	assert("absc" in ind)
	assert(not ind.IsMatch(""))
	assert(not ind.IsMatch("01234"))
	assert(not ind.IsMatch("ASV"))
}