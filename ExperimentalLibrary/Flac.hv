FlacTest := !() -> void
{
	tstData := FSGetFile("tetris.flac").GetBlob()

	eng := SoundEngine()
	eng.CreateTask()
	snd := eng.LoadFromFLAC(tstData)
	// snd.buf = resIdx
	eng.PlaySound(snd).AwaitCompletion()
	eng.Stop()
}


